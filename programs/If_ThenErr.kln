(* This program doesn't work because an if statement needs a then after *)
function BMI(height_inches : integer, weight : integer): integer
    if height_inches = 0
        print(sqr_height(height) * weight)
    else sqr_height(height) * weight

function sqr_height(height : integer): integer
    height * height

function main(height_inches : integer, weight : integer): integer
    BMI(height_inches, weight)
